import React from 'react';
import PropTypes from 'prop-types';


const Date = ({ value }) => {
  return (
    <div>

    </div>
  );
};

Date.propTypes = {
  value: PropTypes.instanceOf(Date),
}


export default Date;