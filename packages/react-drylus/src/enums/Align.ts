// import Enum from '@drawbotics/enums';

// const Align = new Enum('LEFT', 'CENTER', 'RIGHT');

// export default Align;

export enum Align {
  LEFT = 'LEFT',
  CENTER = 'CENTER',
  RIGHT = 'RIGHT',
}
