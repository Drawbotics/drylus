{
  "name": "@drawbotics/style-vars",
  "version": "2.1.1",
  "description": "Style variables used by Drawbotics across different projects",
  "license": "UNLICENSED",
  "main": "dist/vars.js",
  "scripts": {
    "clean": "rimraf dist/",
    "build": "npm run clean && babel src/vars.js --out-file src/vars-build.js && node src/build.js",
    "sync": "node scripts/sync.js",
    "build:dev": "npm run build && NODE_ENV=development npm run sync",
    "prepublishOnly": "npm run build && NODE_ENV=production npm run sync"
  },
  "dependencies": {
    "fs-extra": "^7.0.0",
    "lodash": "^4.17.10",
    "tinycolor2": "^1.4.1"
  },
  "publishConfig": {
    "access": "public"
  },
  "gitHead": "b180a6a41c82c0ae1e6a38e4d1c17e97c5efadde"
}
