import { Layout, Position } from '@drawbotics/react-drylus';

import Playground, { PropsTable } from '~/components/Playground';


export const Sidebar = ({ width }) => {
  return (
    <div style={{ width: width || 200, height: '100%', background: 'red', padding: 20 }}>Sidebar</div>
  );
}

export const Navbar = () => {
  return (
    <div style={{ height: 60, width: '100%', background: 'blue', padding: 20 }}>Navbar</div>
  );
}

export const BottomBar = () => {
  return (
    <div style={{ height: 60, width: '100%', background: 'pink', padding: 20 }}>Bottom bar</div>
  );
}


# Layout component
Usage
```jsx
import { Layout } from '@drawbotics/react-drylus';
```

__Examples__
<PropsTable component={Layout} />

Example with fixed navbar and standard bottom bar

<Playground>
  <div style={{ height: 400 }}>
    <Layout bar={<Navbar />} position={Position.TOP} fixed>
      <Layout bar={<BottomBar />} position={Position.BOTTOM}>
        <div style={{ background: 'green', height: 500, padding: 20 }}>Content</div>
      </Layout>
    </Layout>
  </div>
</Playground>

Example like Backoffice layout

<Playground>
  <div style={{ height: 400 }}>
    <Layout bar={<Sidebar />} position={Position.LEFT} fixed>
      <Layout bar={<Navbar />} position={Position.TOP}>
        <div style={{ background: 'green', height: 500, padding: 20 }}>Content</div>
      </Layout>
    </Layout>
  </div>
</Playground>


Example with sidebar and floating bottom bar

<Playground>
  <div style={{ height: 400 }}>
    <Layout bar={<BottomBar />} position={Position.BOTTOM} fixed>
      <Layout bar={<Navbar />} position={Position.TOP}>
        <Layout bar={<Sidebar width={400} />} position={Position.RIGHT}>
          <div style={{ background: 'green', height: 500, padding: 20 }}>Content</div>
        </Layout>
      </Layout>
    </Layout>
  </div>
</Playground>
