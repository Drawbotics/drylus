import { CircularProgress } from '@drawbotics/react-drylus';

import Playground from '~/components/Playground';


# Circular Progress
Similar to [`ProgressBar`](/component-kit/components/progress-bar) but in a circle. It can display some data also in the middle.
```jsx
import { CircularProgress } from '@drawbotics/react-drylus';
```

__Example__
<Playground component={CircularProgress}>
  <CircularProgress percentage={0.6} />
</Playground>
