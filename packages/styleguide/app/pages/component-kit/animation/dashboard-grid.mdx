import { css } from 'emotion';
import {
  Content,
  Margin,
  Size,
  Title,
  Padding,
  Text,
  Flex,
  FlexItem,
  Grid,
  GridItem,
  FlexSpacer,
  SelectFilter,
  Shade,
  Tile,
  Direction,
  Speed,
} from '@drawbotics/react-drylus';

import Playground from '~/components/Playground';
import { Navbar } from './components/Navbar';

# Dashboard grid

export const Item = () => {
  return (
    <Tile noPadding>
      <img
        src={'https://www.brusselstimes.com/wp-content/uploads/2019/07/newtowertallestbrusselsrueloiperspectivebrussels.jpg'}
        style={{ width: '100%', height: 150, objectFit: 'cover' }} />
      <Padding size={Size.SMALL}>
        <Margin size={{ bottom: Size.EXTRA_SMALL }}>
          <Text>A cool project</Text>
        </Margin>
        <Text size={Size.SMALL} shade={Shade.MEDIUM}>40% sold</Text>
      </Padding>
    </Tile>
  );
};

<Playground mode="jsx">
  <>
    <Navbar />
    <Content>
      <Padding>
        <Margin size={{ top: Size.DEFAULT, bottom: Size.SMALL }}>
          <Title size={2} noMargin>
            Welcome Amber
          </Title>
        </Margin>
        <Text>
          Start by creating a projet
        </Text>
        <Margin size={{ top: Size.EXTRA_LARGE, bottom: Size.LARGE }}>
          <Flex animated animationDirection={Direction.TOP_DOWN} animationSpeed={Speed.FAST}>
            <FlexItem>
              <Title size={4} noMargin>
                Projects
              </Title>
            </FlexItem>
            <FlexSpacer flex size={Size.SMALL} />
            <FlexItem>
              <SelectFilter
                label="Filter"
                onChange={() => {}}
                onClear={() => {}}
                options={[]}
              />
            </FlexItem>
            <FlexSpacer size={Size.SMALL} />
            <FlexItem>
              <SelectFilter
                label="Filter"
                onChange={() => {}}
                onClear={() => {}}
                options={[]}
              />
            </FlexItem>
            <FlexSpacer size={Size.SMALL} />
            <FlexItem>
              <SelectFilter
                label="Filter"
                onChange={() => {}}
                onClear={() => {}}
                options={[]}
              />
            </FlexItem>
          </Flex>
        </Margin>
        <Grid animated hGutters={Size.DEFAULT} vGutters={Size.DEFAULT} columns={4}>
          {[...Array(12).keys()].map((i) =>
            <GridItem key={i}>
              <Item />
            </GridItem>
          )}
        </Grid>
      </Padding>
    </Content>
  </>
</Playground>