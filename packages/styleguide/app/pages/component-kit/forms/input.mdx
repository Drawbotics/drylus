import {
  Input,
  Select,
  Button,
  Icon,
  Flex,
  FlexItem,
  Margin,
  Size,
  FlexJustify,
  Category,
} from '@drawbotics/react-drylus';
import { css } from 'emotion';

import Playground from '~/components/Playground';
import { ControlledField } from '../utils';


# Input
Usage
```jsx
import { Input } from '@drawbotics/react-drylus';
```

__Example__
<Playground component={Input}>
  <ControlledField initialValue="" style={{ maxWidth: 250 }} component={
    <Input placeholder="Placeholder..." onChange={x=>x} value="" />
  } />
</Playground>

The `type` property has been limited to a few possible from the HTML definition, since we have specific components for some types (see the rest of the Forms section). For each type you define, you can pass the native properties to customize it, e.g. for `type="email"` you may pass `pattern=".+@globex.com" size="30"`

If no `onChange` value is passed, the field is automatically set to `read-only`, which doesn't allow the value to be modified.

<Playground>
  <Input value="The value is locked" style={{ maxWidth: 300 }} />
</Playground>

You can also add a leading and trailing components.

export const style = {
  example: css`
    label: noreplace;
    width: 300px;
    > * {
      margin-bottom: 20px !important;
    }
  `,
};

export const Example = ({ children }) => {
  return (
    <div className={style.example}>
      {children}
    </div>
  );
}

<Playground>
  <Example>
    <Input placeholder="Something..." leading="$" value="" onChange={x=>x} />
    <Input placeholder="Something..." leading={<Icon name="command" />} value="" onChange={x=>x} />
    <Input placeholder="Something..." trailing=".com" value="" onChange={x=>x} />
    <Input placeholder="Something..." leading={
      <Select value="option" options={[{ label: 'Option', value: 'option' }]} placeholder="Select..." onChange={x=>x} />
    } value="" onChange={x=>x} />
    <Input placeholder="Something else..." trailing={
      <Button leading={<Icon name="copy" />}>Click me</Button>
    } value="" onChange={x=>x} />
  </Example>
</Playground>


You can use the smaller size variant when space is scarce on the pace (this will be defined in the designs).
<Playground>
  <Example>
    <Input value="Locked" size={Size.SMALL} />
    <Input valid onChange={() => {}} value="Some value" size={Size.SMALL} />
    <Flex justify={FlexJustify.START}>
      <FlexItem flex>
        <Margin size={{ right: Size.EXTRA_SMALL }}>
          <Input onChange={() => {}} placeholder="Type something..." size={Size.SMALL} />
        </Margin>
      </FlexItem>
      <FlexItem>
        <Button category={Category.INFO} size={Size.SMALL}>Click me</Button>
      </FlexItem>
    </Flex>
    <Input size={Size.SMALL} placeholder="Something..." leading={<Icon name="command" />} value="" onChange={x=>x} />
    <Input size={Size.SMALL} placeholder="Something..." trailing=".com" value="" onChange={() => {}} />
    <Input size={Size.SMALL} placeholder="Something..." leading={
      <Select value="option" options={[{ label: 'Option', value: 'option' }]} placeholder="Select..." onChange={x=>x} />
    } value="" onChange={() => {}} />
    <Input size={Size.SMALL} placeholder="Something else..." trailing={
      <Button leading={<Icon name="copy" />}>Click me</Button>
    } value="" onChange={() => {}} />
  </Example>
</Playground>