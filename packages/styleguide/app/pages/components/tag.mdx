import { Tag, Flex, FlexItem, FlexJustify, Sizes } from '@drawbotics/react-drylus';

import Playground from '~/components/Playground';


# Tag
Used to display a status, or a category, but in a more compact way. Tag colors are also lighter, since more than one can be placed together, unlike `Status` which should be standalone.
```jsx
import { Tag } from '@drawbotics/react-drylus';
```

__Example__
<Playground component={Tag}>
  <Tag>Content</Tag>
</Playground>


If you pass a `onClickRemove` function, then a "x" is shown:
<Playground>
  <Flex justify={FlexJustify.START} vSpacing={Sizes.EXTRA_SMALL}>
    <FlexItem>
      <Tag onClickRemove={x=>x}>Removable</Tag>
    </FlexItem>
    <FlexItem>
      <Tag onClickRemove={() => console.log('clicked')} inversed>Another one</Tag>
    </FlexItem>
  </Flex>
</Playground>
