import { Badge } from '@drawbotics/react-drylus';

import Playground from '~/components/Playground';


# Badge
Used to display number of items in a minimal form, usually for notifications. You should never have more than 1 adjacent badge components.

```jsx
import { Badge } from '@drawbotics/react-drylus';
```
__Example__

<Playground component={Badge}>
  <Badge value={30} />
</Playground>
