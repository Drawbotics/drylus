import {
  BaseFilter,
  FilterAlign,
  SelectFilter,
  CheckboxFilter,
  Avatar,
  Sizes,
  Categories,
  Flex,
  FlexItem,
  FlexJustify,
  Icon,
  Margin,
} from '@drawbotics/react-drylus';

import Playground from '~/components/Playground';
import { ControlledField, ControlledMultiField } from '../utils';


# Filter

A filter uses a number of other UI elements. For example, we may use a date picker to filter by date, or a list of checkboxes to filter by a particular criterium. The component exposes the `BaseFilter`, which is used to create custom filters, but also provides pre-built common components, see the examples below for more details.
```jsx
import { BaseFilter, SelectFilter, CheckboxFilter, FilterAlign } from '@drawbotics/react-drylus';
```

__Example__
<Playground component={BaseFilter} enums={{ FilterAlign }}>
  <div style={{ height: 200, marginLeft: 100 }}>
    <BaseFilter label="Base filter" onClear={x=>x}>
      <div style={{ height: 70, background: 'red', padding: 20 }}>
        Some custom content
      </div>
    </BaseFilter>
  </div>
</Playground>

#### Examples

`SelectFilter` inherits all props from `BaseFilter` and adds the following

<Playground component={SelectFilter}>
  <div style={{ height: 200 }}>
    <Flex justify={FlexJustify.END}>
      <FlexItem>
        <Margin size={{ right: Sizes.SMALL }}>
          <ControlledField component={
            <SelectFilter
              label="Country"
              onClear={x=>x}
              onChange={x=>x}
              options={[{
                leading: <Avatar text="Belgium" backgroundColor="#E00D2B" size={Sizes.SMALL} />,
                label: 'Belgium',
                value: 'BE',
              }, {
                leading: <Avatar text="France" backgroundColor="#011082" size={Sizes.SMALL} />,
                label: 'France',
                value: 'FR',
              }, {
                leading: <Avatar text="Italy" backgroundColor="#128435" size={Sizes.SMALL} />,
                label: 'Italy',
                value: 'IT',
              }]} />
          } />
        </Margin>
      </FlexItem>
      <FlexItem>
        <ControlledField component={
          <SelectFilter
            label="Notifications"
            onClear={x=>x}
            onChange={x=>x}
            options={[{
              leading: <Icon name="x" category={Categories.DANGER} />,
              label: 'Dismissed',
              value: 'dismissed',
            }, {
              leading: <Icon name="bell" category={Categories.WARNING} />,
              label: 'Snoozed',
              value: 'snoozed',
            }, {
              leading: <Icon name="check" category={Categories.SUCCESS} />,
              label: 'Seen',
              value: 'seen',
            }]} />
        } />
      </FlexItem>
    </Flex>
  </div>
</Playground>


`CheckboxFilter` inherits all props from `BaseFilter` and adds the following

<Playground component={CheckboxFilter}>
  <div style={{ height: 200, marginLeft: 100 }}>
    <ControlledMultiField component={
      <CheckboxFilter
        align={FilterAlign.LEFT}
        label="Products"
        onClear={x=>x}
        onChange={x=>x}
        options={[{
          label: 'Exterior 3D',
          value: 'exterior3d',
        }, {
          label: 'Interior 3D',
          value: 'interior3d',
        }, {
          label: 'Shoebox',
          value: 'shoebox',
        }, {
          label: 'Website',
          value: 'website',
        }]} />
    } />
  </div>
</Playground>
