import { TabNavigation, Flex, FlexItem, FlexJustify } from '@drawbotics/react-drylus';
import sv from '@drawbotics/style-vars';

import Playground from '~/components/Playground';
import { ControlledField } from '../utils';


# Tab Navigation

It is primarily used to change the context of a whole page, that is in contrast with `SegmentedControl` which only should modify part of a page's content.

This is the most basic usage of a tab navigation:

<Playground component={TabNavigation}>
  <ControlledField component={
    <TabNavigation
      options={[{
        label: 'First choice',
        value: 'first',
      }, {
        label: 'Second choice',
        value: 'second',
      }, {
        label: 'Third',
        value: 'third',
        bullet: 4,
        disabled: true,
      }]} />
  } />
</Playground>

However it can also be set vertically, and each value may have bullets (similarly to `SegmentedControl`), and may also be disabled.

<Playground>
  <div style={{ maxWidth: 300, background: sv.neutralLighter }}>
    <ControlledField component={
      <TabNavigation
        vertical
        options={[{
          label: 'First choice',
          value: 'first',
          bullet: 4,
        }, {
          label: 'Second choice',
          value: 'second',
          bullet: 10,
          disabled: true,
        }, {
          label: 'Third',
          value: 'third',
          bullet: 123,
        }, {
          label: 'Fourth',
          value: 'fourth',
          bullet: 40,
        }]} />
      } />
  </div>
</Playground>


Examples with link component wrapping
<Playground>
  <Flex justify={FlexJustify.SPACE_AROUND}>
    <FlexItem>
      <div style={{ width: 250, background: sv.neutralLighter }}>
        <ControlledField component={
          <TabNavigation
            linkComponent="a"
            vertical
            options={[{
              label: 'First choice',
              value: 'first',
              bullet: 4,
            }, {
              label: 'Second choice',
              value: 'second',
              bullet: 10,
              disabled: true,
            }, {
              label: 'Third',
              value: 'third',
              bullet: 123,
            }, {
              label: 'Fourth',
              value: 'fourth',
              bullet: 40,
            }]} />
          } />
      </div>
    </FlexItem>
    <FlexItem>
      <ControlledField component={
        <TabNavigation
          linkComponent="a"
          options={[{
            label: 'First choice',
            value: 'first',
          }, {
            label: 'Second choice',
            value: 'second',
          }, {
            label: 'Third',
            value: 'third',
            bullet: 4,
            disabled: true,
          }]} />
      } />
    </FlexItem>
  </Flex>
</Playground>
