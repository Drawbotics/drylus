import {
  Button,
  Tiers,
  Sizes,
  Categories,
  Icon,
  Spinner,
  Flex,
  FlexItem,
  FlexJustify,
} from '@drawbotics/react-drylus';

import Playground from '~/components/Playground';


# Button

Usage
```jsx
import { Button } from '@drawbotics/react-drylus';
```
__Example__
<Playground component={Button}>
  <Button>Button Text</Button>
</Playground>


You can also have buttons with just the icon, or just the spinner; for this don't pass any children. "No Children" buttons only support default and small size.
<Playground>
  <Flex justify={FlexJustify.START} vSpacing={Sizes.SMALL}>
    <FlexItem>
      <Button leading={<Icon name="command" />} tier={Tiers.TERTIARY} />
    </FlexItem>
    <FlexItem>
      <Button tier={Tiers.TERTIARY} leading={<Spinner category={Categories.INFO} size={Sizes.SMALL} />} />
    </FlexItem>
    <FlexItem>
      <Button leading={<Icon name="command" />} size={Sizes.SMALL} />
    </FlexItem>
    <FlexItem>
      <Button leading={<Icon name="command" />} size={Sizes.SMALL}>Small with text</Button>
    </FlexItem>
    <FlexItem>
      <Button leading={<Icon name="command" />} tier={Tiers.SECONDARY} />
    </FlexItem>
    <FlexItem>
      <Button leading={<Icon name="command" />} category={Categories.BRAND}>Brand with text</Button>
    </FlexItem>
    <FlexItem>
      <Button trailing={<Spinner size={Sizes.SMALL} inversed />} category={Categories.SUCCESS}>Text with spinner</Button>
    </FlexItem>
  </Flex>
</Playground>
