import { SegmentedControl } from '@drawbotics/react-drylus';

import Playground from '~/components/Playground';
import { ControlledField } from '../utils';


# Segmented Control

<Playground component={SegmentedControl}>
  <ControlledField component={
    <SegmentedControl
      values={[{
        label: 'First choice',
        value: 'first',
      }, {
        label: 'Second choice',
        value: 'second',
      }, {
        label: 'Third',
        value: 'third',
      }]} />
  } />
</Playground>


You can also have a value displayed within each control:

<Playground>
  <ControlledField component={
    <SegmentedControl
      values={[{
        label: 'First choice',
        value: 'first',
        bullet: 4,
      }, {
        label: 'Second choice',
        value: 'second',
        bullet: 23,
      }, {
        label: 'Third',
        value: 'third',
        bullet: 123,
      }]} />
  } />
</Playground>
